<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 亮色皮肤 -->
    <style name="Skin.Light" parent="BaseActivityTheme">
        <item name="colorPrimary">@color/background</item>
        <item name="colorPrimaryDark">@color/background</item>
        <item name="colorAccent">@color/background</item>
        <item name="kThemeBackground">@color/background</item>

    </style>
    <style name="Skin.Light.NoActionBar" parent="Skin.Light">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <!-- 暗色皮肤 -->
    <style name="Skin.Dark" parent="BaseActivityTheme">
        <item name="colorPrimary">@color/abc_search_url_text</item>
        <item name="colorPrimaryDark">@color/abc_search_url_text</item>
        <item name="colorAccent">@color/abc_search_url_text</item>
        <item name="kThemeBackground">@color/abc_search_url_text</item>
    </style>
    <style name="Skin.Dark.NoActionBar" parent="Skin.Dark">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

</resources>